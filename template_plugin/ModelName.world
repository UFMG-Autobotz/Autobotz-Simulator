<?xml version="1.0" ?>
<sdf version="1.5">
  <world name="default">
    <!-- A global light source -->
    <include>
      <uri>model://sun</uri>
    </include>
    <!-- A ground plane -->
    <include>
      <uri>model://ground_plane</uri>
    </include>

    <!-- A testing model that includes the ModelName sensor model -->
    <model name="ModelName">
      <include>
        <uri>model://ModelName</uri>
      </include>

      <!-- Attach the plugin to this model -->
      <plugin name="ModelName_control" filename="libmodelname_plugin.so"/>
    </model>

  </world>
</sdf>
